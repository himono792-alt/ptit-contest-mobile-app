// Sprint 28 (2026-05-09): smoke test stub minimal cho project ptit_contest.
// Default Flutter Counter test stub đã loại bỏ vì khong khớp project type.
//
// Khi cần unit test thật:
//   flutter test test/<feature>_test.dart
//
// Hiện chưa có test suite — defer Sprint 30+ với test pyramid (unit + widget + integration).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('smoke test placeholder — pass', () {
    expect(2 + 2, equals(4));
  });
}
